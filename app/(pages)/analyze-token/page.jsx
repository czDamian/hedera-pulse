import AnalyzeToken from "./AnalyzeToken";
export const metadata = {
  title: "Analyze Token -Hedera Pulse",
  description: "Your AI Token Launcher on Hedera Blockchain",
};

const page = () => {
  return (
    <div>
      <AnalyzeToken />
    </div>
  );
};

export default page;
